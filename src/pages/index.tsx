import clsx from "clsx";

export default function HomePage() {
  return (
    <section className={clsx("container mx-auto h-screen p-6", "flex flex-col items-center justify-center")}>
      <h1>Devix.id</h1>
    </section>
  );
}
